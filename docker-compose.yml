services:
  backend:
    build: ../levise-backend
    ports:
      - "8000:8000"
    env_file:
      - ../levise-backend/.env
    environment:
      - ALLOWED_HOSTS=localhost,127.0.0.1,backend
      - CORS_ALLOWED_ORIGINS=http://localhost:3000
    volumes:
      - ../levise-backend:/app
      - backend-db:/app/db
    command: >
      sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"

  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      - backend

volumes:
  backend-db:
